git config --global user.name "" 
git config --global user.email "" 

git add .
git commit -m ""
git status
git branch -M main 
git checkout -b "verRamaACrear"     git branch "nuevaRama"
                                    git checkout "ramaQueQuieroVer" 
git merge "ramaQueSeUniráALaActual" -m "" 
$ git branch -d ramaABorrar

gitk --all 